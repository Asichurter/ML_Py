训练集（均无padding，转换为默认值）：
恶性：每个类除worm与virus外，各取70个样本，不足70个取一半，共1055个
良性：400个取自windows（随机抽样），600个取自C:/Program Files（生成器生成），然后分出200个作为验证集（随机抽样），共796个
验证集：
恶性：均取自virus类，共201个（随机抽样）
良性：从训练集中分出，共199个

训练：
Train Batch=48（显存约束）
Validate Batch=16
Resnet18
Epoch=30
Adam lr=1e-3 weight_decay=1e-4
BCELoss
依据指标调整学习率
指标：val_loss 耐心=1 系数=0.5 最小学习率=1e-5
提前终止：False